#include "main.h"

/**
 * absolute_value - Compute the absolute value of an integer.
 * @n: The integer to find the absolute value of.
 *
 * Return: The absolute value of the integer.
 */

int absolute_value(int n)
{
if (n >= 0)
{
(return n);
}
(return -n);
}

